// deno-lint-ignore-file no-unused-vars
import { Value, Tabula, Result } from '../../types.ts'

export const bare = (tabula: Tabula, context?: Tabula): Value | Result => {
  //TODO: Implement
  return
}